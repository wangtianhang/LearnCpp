#version 450 core
in vec4 position;
out VS_OUT
{
vec4 color;
} vs_out;
uniform mat4 mvp_matrix;
void main(void)
{
vec4 tmpPosition = position;
tmpPosition.w = 1; // 向量w为0 只进行旋转 不进行位移
gl_Position = mvp_matrix * position;
vs_out.color = position * 2.0 + vec4(0.5, 0.5, 0.5, 0.0);
}